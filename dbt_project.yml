name: 'new_first_project'   # 👈 This must match your dbt Cloud project name
version: '1.0'
config-version: 2

profile: 'default'          # 👈 Use your connection profile (e.g., for Snowflake)

model-paths: ["models"]
seed-paths: ["seeds"]

seeds:
  new_first_project:        # 👈 Must match the `name:` value above
    country_codes:
      column_types:
        country_code: string
        country_name: string
